spring:
  application:
    name: devices-access
  cloud:
    config:
      profile: dev
      uri:  http://114.115.130.42:1111
#      discovery:
#        enabled: true
#        service-id: config
server:
  port: 8083
eureka:
  client:
    service-url:
      defaultZone: http://114.115.130.42:8761/eureka/
  instance:
    prefer-ip-address: true
    instance-id: 114.115.130.42:${spring.application.name}:${server.port}

